spring:
  application:
    name: avizandum
  cloud:
    vault:
      host: thor
      port: 8200
      scheme: http
      authentication: TOKEN
      token: ${myvaulttoken}
      kv:
        backend-version: 3
        enabled: true
